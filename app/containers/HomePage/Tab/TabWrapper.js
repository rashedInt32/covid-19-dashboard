import styled from 'styled-components';

const TabWrapper = styled.div`
  posistion: relative;
  display: flex;
  align-items: center;
  padding-top: 40px;
  flex-wrap: wrap;
`;

export default TabWrapper;
